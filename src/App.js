// Imported Body component from the Components folder.
import Body from "./Components/Body";
// Imported Header component from the Components folder.
import Header from "./Components/Header";

function App() {
  return (
    <div className="main-container">
      <Header />
      <Body />
    </div>
  );
};

// The App function is exported.
export default App;
